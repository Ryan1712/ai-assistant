# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# UI/UX

Before touching any screen or component, read `DESIGN.md` (design guideline) and use tokens from `src/ui/theme.ts` — never inline hex colors or off-grid spacing values.
